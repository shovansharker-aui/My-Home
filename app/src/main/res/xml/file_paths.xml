<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="previews" path="camera_previews/" />
    <!-- Share downloads the original into the cache first when it isn't in
         the preview cache yet. Without this root, FileProvider.getUriForFile
         threw and took the app down with it. -->
    <cache-path name="shared" path="shared/" />
</paths>
